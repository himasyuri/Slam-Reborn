<UserControl x:Class="SlamReborn.Components.TracksListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SlamReborn.Components" xmlns:viewmodels="clr-namespace:SlamReborn.ViewModels" d:DataContext="{d:DesignInstance Type=viewmodels:TracksListViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="700">
    <Grid>
        <!--<ListView ItemsSource="{Binding TracksListItemViewModels}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <local:TracksListViewItem/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->
        <ListView     Width="700" 
                      Height="450">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                
            </Grid>

            <!--<ListView.ItemTemplate>
                <DataTemplate>
                    <local:TracksListViewItem/>
                </DataTemplate>
            </ListView.ItemTemplate>
            </ListView>-->
        </ListView>
    </Grid>
</UserControl>
